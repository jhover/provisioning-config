KERBEROS ^([^@/]*)@(.*)$ \1@\2
<% @worker_nodes.each do |wn| -%>
GSI <%= @gsi_dn_prefix + Regexp.escape(wn) + @gsi_dn_suffix -%> worker-node@<%= @uid_domain %>
<% end -%>
<% @computing_elements.each do |ce| -%>
GSI <%= @gsi_dn_prefix + Regexp.escape(ce) + @gsi_dn_suffix -%> computing-element@<%= @uid_domain %>
<% end -%>
<% @managers.each do |cm| -%>
GSI <%= @gsi_dn_prefix + Regexp.escape(cm) + @gsi_dn_suffix -%> central-manager@<%= @uid_domain %>
<% end -%>
FS (.*) \1@fsauth
