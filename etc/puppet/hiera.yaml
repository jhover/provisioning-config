#
# Placed in /etc/puppet because in certain contexts it cannot be specified. 
#
---
:backends:
  - yaml

:yaml:
  :datadir: /etc/hiera

:merge_behavior:
  - deeper
  
:hierarchy:
   - userdata
   - local
   - defaults

